name: esimnest
description: eSIMNest - Global Data eSIM
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'  # ← FIXED: Added SDK constraint

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.1.0
  js: ^0.7.1
  firebase_core: ^3.12.0
  firebase_auth: ^5.5.0
  cloud_firestore: ^5.6.0
  google_sign_in: ^6.3.0
  http: ^1.6.0
  url_launcher: ^6.3.2
  intl: ^0.20.0
  provider: ^6.1.5+1
  razorpay_flutter: ^1.3.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png
    - assets/images/splash.png
    - assets/images/banner.png
